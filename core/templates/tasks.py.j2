from core.celery_app import celery_app
from config.logging import logger

# Celery tasks for the '{{name}}' app.

# To create a new task, define a function and decorate it with @celery_app.task.
# You can then call this task asynchronously from your application code
# using `my_task.delay(arg1, arg2)`.

# Example:
#
# @celery_app.task(name="{{name}}.process_data")
# def process_data_task(data_id: int):
#     """
#     An example task that processes some data.
#     """
#     logger.info(f"Processing data with ID: {data_id} in app '{{name}}'")
#     # ... your time-consuming logic here ...
#     return f"Data {data_id} processed successfully."